#include <stdio.h>



int main()
{
	
	int v1[2];
	int v2[2];
	int v3[2];
	int cont;
	int* v[3] = {v1, v2, v3 };
	
	int i;
	for(i = 0; i < 3; i++)
	{
		scanf("%i %i", &(*(v + i)[0]), &(*(v + i)[1]));
	}
	for(i = 0; i < 3; i++)
	{
		int cont = 0;
		if()
		{
		cont++;	
		}
		if("quantidade"0 do vetor i > 0)
		{
		cont++;	
		}
	}
	return 0;
}