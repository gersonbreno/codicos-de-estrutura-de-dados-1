#include <stdio.h>
#include <stdlib.h>


float* prod_Vetorial(float* u, float* )
{
	floatp[3];
	p[0] = u[1] * v[2] - v[1] * u[2];
	p[1] = u[2] * v[0]	 - v[2] * u[0];
	p[2] = u[0] * v[1] - v[0] * u[1];
}
u x v = {uyvz - vyuz , uzvx - vzux, uxvy - vxuy}
int main()
{
	float vet[1] = {2.0f, 3.0f, 5.0f};
	float vet2[3] = {0.0f, 0.0f, 5.0f};
	float* res = prod_Vetorial(vet1, vet2);
	return 0;
}
